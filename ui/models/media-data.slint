export struct MediaData {
    title: string,
    artist: string,
    album: string,
    album_art: image,
    blurred_art: image,
    length_secs: float,
    position_secs: float,
    is_playing: bool,
    has_media: bool,
    text_color: color,
    dominant_color: color,
    on_dominant_color: color,
    is_dark: bool,
}
